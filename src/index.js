const axios = require("axios"); module.exports = async (link) => { try { const res = await axios.get(`https://api.alyachan.my.id/api/fb?url=${ encodeURIComponent(link) }`); if (res.data.status) { try { return res.data.data.find(item => item.quality.includes('HD'))?.url; } catch (e) { return res.data.data.find(item => item.quality.includes('HD'))?.url; }; } else { return "Video not found, maybe it's privated video"; }; } catch (error) { return "Video not found, maybe it's privated video"; }; };